# 📦 Название Проекта

Краткое описание в одно-два предложения: какую задачу решает этот проект, какие технологии или стандарты (например, C++20) он закрепляет.

## 🛠 Технологический стек & Архитектура

* **Стандарт:** C++20
* **Система сборки:** CMake + CTest
* **Тестирование:** Google Test (GTest)
* **Специфика:** *(например: RAII, move-семантика, stateless функциональные объекты, кастомная vtable и т.д.)*

## 🚀 Сборка и запуск

Сборка проекта выполняется стандартными командами:

```bash
cmake -S . -B build -DBUILD_TESTS=ON
cmake --build build
ctest --test-dir build --output-on-failure
```
